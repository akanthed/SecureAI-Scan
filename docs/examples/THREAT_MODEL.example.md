<!--
  Example output, committed as-is (only this comment block and the "Scanned"
  path below are edited — every finding, table, and remediation line past
  this point is real, unmodified `secureai-scan threat-model` output).

  Generated from examples/vulnerable.ts, the small illustrative file this
  repo already ships for demos — not a third-party repo, so nothing here
  characterizes anyone else's code. Regenerate against your own project:

    secureai-scan threat-model . --output THREAT_MODEL.md

  This is the artifact behind the "compliance packaging" pitch in
  MARKETING.md and ROADMAP.md: the OWASP LLM/ASI/MCP Top 10 coverage
  matrix below is the same shape an EU AI Act Article 15 documentation
  request or an internal security review asks for, generated offline in
  under a second, no account and no manual mapping required.
-->

# Threat Model — AI/LLM Security

> Generated by SecureAI-Scan v0.10.0 on 8/26/2026
> Scanned: `examples/`

---

## Executive Summary

| | |
|---|---|
| **Security Grade** | **F** |
| Total Findings | 2 |
| Critical | 2 |
| High | 0 |
| Medium | 0 |
| Low | 0 |

## OWASP Framework Coverage

How this scan maps onto the three OWASP AI security frameworks. "Runtime concern" marks risks a static scanner cannot assess — cover those with runtime controls.

### OWASP Top 10 for LLM Applications (2026)

| Risk | Name | Rules | Status |
|------|------|-------|--------|
| LLM01 | Prompt Injection | `AI001`, `AI007`, `AI010`, `MCP001`, `MCP007`, `MCP008`, `MCP009`, `SKL001`, `SKL002`, `SKL003` | 🟢 covered, no findings |
| LLM02 | Sensitive Information Disclosure | `AI002`, `AI004`, `MCP005`, `SKL005`, `LLC001` | 🟢 covered, no findings |
| LLM03 | Excessive Agency | `AI006`, `AI011`, `SKL007`, `LLC003` | 🟢 covered, no findings |
| LLM04 | Supply Chain | `MCP002`, `MCP004`, `MCP006`, `MCP010`, `SKL004`, `SKL006`, `SKL008`, `SKL010`, `DEP001`, `DEP002`, `DEP003`, `LLC002` | 🟢 covered, no findings |
| LLM05 | Data and Model Poisoning | `SKL009`, `VEC003` | 🟢 covered, no findings |
| LLM06 | Unbounded Consumption | `AI003`, `AI009`, `VEC002` | 🔴 findings in this scan |
| LLM07 | Misinformation | — | ⚪ runtime concern (output monitoring) |
| LLM08 | Hidden Context Exposure | `AI008` | 🟢 covered, no findings |
| LLM09 | Vector and Embedding Weaknesses | `VEC001`, `VEC004` | 🟢 covered, no findings |
| LLM10 | Improper Output Handling | `AI005`, `AI012`, `MCP003` | 🟢 covered, no findings |

### OWASP Top 10 for Agentic Applications (2026)

| Risk | Name | Rules | Status |
|------|------|-------|--------|
| ASI01 | Agent Goal Hijack | `AI001`, `AI010`, `MCP001`, `MCP007`, `MCP008`, `SKL001`, `SKL002` | 🟢 covered, no findings |
| ASI02 | Tool Misuse & Exploitation | `AI006`, `MCP003`, `MCP009`, `SKL003` | 🟢 covered, no findings |
| ASI03 | Identity & Privilege Abuse | `AI003`, `SKL007` | 🔴 findings in this scan |
| ASI04 | Agentic Supply Chain Vulnerabilities | `MCP002`, `MCP004`, `SKL004`, `SKL005`, `SKL008`, `DEP001`, `DEP002`, `DEP003` | 🟢 covered, no findings |
| ASI05 | Unexpected Code Execution | `AI005`, `AI012`, `MCP010`, `SKL006`, `SKL010` | 🟢 covered, no findings |
| ASI06 | Memory & Context Poisoning | `AI007`, `SKL009`, `VEC003` | 🟢 covered, no findings |
| ASI07 | Insecure Inter-Agent Communication | `AI011` | 🟢 covered, no findings |
| ASI08 | Cascading Failures | — | ⚪ runtime concern (orchestration monitoring) |
| ASI09 | Human-Agent Trust Exploitation | — | ⚪ process concern (UX / approval design) |
| ASI10 | Rogue Agents | — | ⚪ runtime concern (behavioral monitoring) |

### OWASP MCP Top 10 (2025)

| Risk | Name | Rules | Status |
|------|------|-------|--------|
| MCP01 | Token Mismanagement & Secret Exposure | `MCP005` | 🟢 covered, no findings |
| MCP02 | Privilege Escalation via Scope Creep | `SKL007` | 🟢 covered, no findings |
| MCP03 | Tool Poisoning | `MCP001`, `MCP003`, `MCP007`, `MCP008`, `MCP009` | 🟢 covered, no findings |
| MCP04 | Software Supply Chain Attacks | `MCP004`, `SKL004`, `SKL005`, `SKL008`, `DEP003` | 🟢 covered, no findings |
| MCP05 | Command Injection & Execution | `MCP010`, `SKL006` | 🟢 covered, no findings |
| MCP06 | Intent Flow Subversion | — | ⚪ no static rule yet |
| MCP07 | Insufficient Authentication & Authorization | `MCP006` | 🟢 covered, no findings |
| MCP08 | Lack of Audit and Telemetry | — | ⚪ runtime concern (audit logging infrastructure) |
| MCP09 | Shadow MCP Servers | `MCP002` | 🟢 covered, no findings |
| MCP10 | Context Injection & Over-Sharing | — | ⚪ no static rule yet |

## Trust Boundaries & Attack Surfaces

The following data flows were identified as potential attack surfaces:

### Unauthenticated Request → LLM Model

**Risk level:** 🔴 CRITICAL | **2 finding(s)**

- `AI003` LLM call in unauthenticated request handler — `vulnerable.ts:10`
- `AI003` LLM call in unauthenticated request handler — `vulnerable.ts:15`

## Attack Scenarios

Based on detected patterns, here are the most realistic attack chains:

### Scenario 1: LLM call in unauthenticated request handler

**Rule:** `AI003` | **Severity:** 🔴 CRITICAL
**Location:** `vulnerable.ts:10`

**What happens:**
> No authentication call, auth middleware, or session check was found between the request entry point and the LLM invocation. If auth is not enforced elsewhere (gateway/middleware), unauthenticated callers can consume paid model capacity and reach LLM-backed functionality.

**How to fix:**
> Authenticate before invoking the model (e.g. requireAuth(req) / verifyJwt / middleware on the route). If auth is enforced upstream, suppress with: // secureai-ignore AI003: auth enforced by <where>.

### Scenario 2: LLM call in unauthenticated request handler

**Rule:** `AI003` | **Severity:** 🔴 CRITICAL
**Location:** `vulnerable.ts:15`

**What happens:**
> No authentication call, auth middleware, or session check was found between the request entry point and the LLM invocation. If auth is not enforced elsewhere (gateway/middleware), unauthenticated callers can consume paid model capacity and reach LLM-backed functionality.

**How to fix:**
> Authenticate before invoking the model (e.g. requireAuth(req) / verifyJwt / middleware on the route). If auth is enforced upstream, suppress with: // secureai-ignore AI003: auth enforced by <where>.

## All Findings by Category

### AI / LLM Security

| Rule | Severity | File | Line | Summary |
|------|----------|------|------|---------|
| `AI003` | CRITICAL | `vulnerable.ts` | 10 | An unresolved LLM client call runs in a request handler with no visible auth check before it. |
| `AI003` | CRITICAL | `vulnerable.ts` | 15 | An unresolved LLM client call runs in a request handler with no visible auth check before it. |

## Remediation Priority

Fix in this order for maximum risk reduction:

1. **LLM call in unauthenticated request handler** (`AI003`) — `vulnerable.ts:10`
   - Authenticate before invoking the model (e.g. requireAuth(req) / verifyJwt / middleware on the route). If auth is enforced upstream, suppress with: // secureai-ignore AI003: auth enforced by <where>.
2. **LLM call in unauthenticated request handler** (`AI003`) — `vulnerable.ts:15`
   - Authenticate before invoking the model (e.g. requireAuth(req) / verifyJwt / middleware on the route). If auth is enforced upstream, suppress with: // secureai-ignore AI003: auth enforced by <where>.

---

*This threat model was auto-generated. Review with your security team before sharing.*
*To re-generate: `secureai-scan threat-model <path>`*